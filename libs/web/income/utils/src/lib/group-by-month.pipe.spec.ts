import { GroupByMonthPipe } from './group-by-month.pipe';

describe('GroupByMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
